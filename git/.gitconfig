# This is Git's per-user configuration file.
# Navn og e-post legges til av install.sh (eksisterende verdier beholdes),
# eller manuelt med:
#   git config --global user.name  "Ditt Navn"
#   git config --global user.email "din@epost.no"
